---
- name: DB2 Cluster Monitoring
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000
  rules:
    - name: Display Event
      condition: event.payload is defined
      action:
        debug:
          msg: "{{ event.payload }}"
